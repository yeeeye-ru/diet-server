{
  "name": "diet-server",
  "version": "1.0.0",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@vercel/kv": "^1.0.1", // Vercel官方KV存储
    "cors": "^2.8.5"       // 跨域支持
  },
  "engines": {
    "node": "24.x"
  }
}
